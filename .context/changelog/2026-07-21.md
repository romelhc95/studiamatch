# 2026-07-21 - Auditoria Y Remediacion Documental De Hito 1

## Cambios Documentales

- Se audito el informe de cumplimiento de Hito 1 v2 contra el commit, Supabase Free y la posta SDLC.
- Se registro [[../evidencias/hito_1_auditoria_cobertura_20260721]] con veredicto `NO_GO` para cierre y PR listo.
- `TAREA-001` paso de `completado` a `observado` sin eliminar el informe ni el QA report historicos.
- Se creo [[../backlog_tareas/req_est_001_sprint_1/tarea_006_hito_1_remediacion_cierre_sdlc]] en estado `pendiente` con findings, alcance, CAs, pruebas y handoffs.
- Se actualizo el indice del requerimiento para exponer la remediacion al dispatcher.

## Remediacion Fase 01 — TAREA-006

### RLS Reconciliation (Finding #3)
- **Drift detectado**: `leads_insert_public` y `leads_insert_authenticated` en Supabase Free carecian de la validacion `course_id` contra cursos publicables. La migracion versionada `20260712_hito1_editorial_quality_contract.sql` si la contenia pero Free se aplico desde una version anterior.
- **Correccion**: Nueva migracion `db/migrations/20260721_hito1_rls_reconciliation.sql` recrea ambas policies con el texto completo versionado (validacion `course_id` + `publication_status='publicado'` + `production_enabled=true`).
- **Verificacion**: Supabase Free confirma ambas policies contienen `publication_status` en su `WITH CHECK`. Paridad Git/Free restaurada.

### Prueba Funcional Orquestador (Finding #5)
- Creado `tests/test_orchestrator_gates.py`: 5 casos que validan:
  - `discovery_enabled=false` se excluye antes del `limit`.
  - `circuit_open=true` se excluye antes del `limit`.
  - `limit` se aplica post-filtro (no pre-filtro).
  - Instituciones sin perfil son excluidas.
  - Metadata de gates se preserva en los dicts retornados.

### Documentacion Canonica (Finding #4)
- `sistema_db_supabase.md`: Agregados 8 campos Hito 1 en `courses` (`publication_status`, `data_quality_status`, `missing_fields`, `field_sources`, `manual_updated_at`, `is_sponsored`, `sponsorship_priority`, `sponsorship_label`) y `lead_source_type` en `leads`. Actualizada seccion RLS con policies completas de leads (validacion `course_id`).
- `arquitectura_pipeline.md`: Documentado que FG1/FG3 estan desactivados por cron (Hito 1 manual-only). Agregada nota de gates pre-`limit` en orquestador.

### Tooling de Gobernanza (Finding #6)
- `tests/test_hito_governance.py` y `scripts/maintenance/validate_hito_close.py` fueron incluidos en el commit original como parte del contrato SDLC de Hito 1. El gate mecanico `validate_hito_close.py` es el unico verificador determinista del cierre de hito exigido por la posta SDLC. `test_hito_governance.py` valida que el gate no tenga bypasses. Ambos son artefactos de infraestructura SDLC, no funcionalidad de negocio, y su inclusion en el PR de Hito 1 es correcta porque sin ellos no hay verificacion mecanica del cierre.

### Limitacion Documental (Finding #7)
- El PDF de requerimiento `requerimientos/30062026/Studiamatch_MVP_Requerimientos_v5.pdf` existe en el repositorio pero no se versiona en el worktree de la rama `feat/hito-1-foundation-v2` porque no fue modificado. La trazabilidad contractual se mantiene via referencia en `TAREA-001` e `hito_1_informe_cumplimiento.md`. Esta es una limitacion residual aceptada: el PDF esta versionado en `origin/desarrollo`.

## Restricciones Preservadas

- No se modifico codigo funcional del pipeline.
- No se aplico DDL en Supabase Pro.
- No se creo PR, no se hizo merge y no se promovio ningun ambiente.
- El merge y la homologacion local solo ocurren despues de aprobacion humana.
