---
fecha: 2026-07-11
tags: [operaciones, estimaciones, backlog, contexto]
---

# 2026-07-11 — Preparacion operativa para Sprint 1 aprobado

## Cambios
- Corregido `.context/crear_tarea.py` para eliminar el `SyntaxError` causado por rutas Windows en docstring.
- Ampliado `crear_tarea.py` para generar tareas con hito, paquete, CAs, fechas, responsable, revisor, aprobador, entregable, dependencias, criterios y archivos previstos.
- Actualizadas referencias obsoletas a `.context/arquitectura/` en `AGENTS.md`, `.context/prompts/system_prompt_base.md` y `.antigravity/rules.md`.
- Reemplazada `_plantilla_estimacion.md` por una plantilla alineada al formato comercial aprobado en `requerimientos/30062026/EstimacionStudIAMatch_05072026.docx`.
- Reemplazada `_plantilla_tarea.md` por una plantilla orientada a control de plazos, entregables y validaciones.
- Marcada `est_001.md` como aprobada por el cliente y vinculada al DOCX comercial aprobado.
- Creado `.context/operaciones/flujo_requerimientos.md` con el proceso recurrente desde requerimiento hasta entrega.
- Actualizado `.context/00_INDICE.md` para incluir el flujo operativo.
- Creadas 5 tareas de backlog desde `est_001.md`, una por hito aprobado del Sprint 1.
- Detallados los tipos de cambio previstos por archivo en cada tarea generada.
- Agregado `desestimado/` a `.gitignore` porque contiene archivos legacy/scratch no esenciales y no debe versionarse.
- Incorporada matriz de skills/agentes y sub-especialidades tecnicas al flujo recurrente de requerimientos.
- Ampliado `crear_tarea.py` y la plantilla de tareas para registrar `skill_principal`, `subespecialidad`, `skills_apoyo` y `gate_obligatorio`.
- Actualizadas las 5 tareas de `est_001.md` con skill principal, sub-especialidad y skills de apoyo.
- Creados agentes globales OpenCode alineados al stack del proyecto: `frontend-architect`, `supabase-architect`, `pipeline-engineer`, `devops-release-manager`, `qa-test-engineer` y `data-quality-analyst`.
- Reasignadas las tareas de `est_001.md` para usar esos agentes tecnologicos como `skill_principal` o apoyo segun el dominio del cambio.
- Refactorizados los agentes globales para que sean reutilizables en otros proyectos y lean el contexto local antes de asumir stack.
- Validado `tech-estimator` como agente exclusivo de StudIAMatch y alineado a la plantilla vigente `.context/estimaciones/_plantilla_estimacion.md` y al DOCX aprobado.
- Documentado que no se deben crear agentes locales `.opencode/agents/` en este repo salvo aprobacion explicita; la asignacion por tecnologia se controla desde `.context` y las tareas.
- Separada explicitamente la plantilla comercial de cliente (`estimaciones/`) del backlog tecnico interno (`backlog_tareas/`).
- Ampliada `_plantilla_tarea.md` para que cada tarea interna incluya alcance incluido/excluido, subtareas tecnicas, validaciones, evidencia y checklist de cierre.
- Ampliado `crear_tarea.py` para generar tareas futuras con `--alcance-incluido`, `--alcance-excluido` y `--subtarea`.
- Detalladas `TAREA-001` a `TAREA-005` con subtareas tecnicas trazadas a CAs, archivos esperados y validaciones por hito.
- Actualizado `operaciones/flujo_requerimientos.md` para exigir tareas internas suficientemente desmenuzadas para ejecucion por agente/desarrollador.
- Reforzado el estandar interno de tareas: cada tarea debe iniciar con analisis tecnico previo y luego documentar especificacion exacta del cambio antes de ejecutar.
- Ampliado `crear_tarea.py` con `--analisis-previo` y `--especificacion-cambio` para nuevos pedidos.
- Refinada `TAREA-001` a nivel implementable: workflows, orquestador, migration SQL propuesta, columnas/tipos/defaults/checks/indices, idempotencia y reglas RLS/RPC.
- Agregadas `Fuentes del requerimiento` y `Matriz CA -> detalle implementable` a la plantilla, generador y tareas para forzar trazabilidad contra documento fuente y mockups aprobados.
- Realineada `TAREA-002` con el PDF v5 secciones 5, 6 y 8.1: la completitud del pipeline se mide por datos criticos visibles del frontend, no por criterios internos legacy del enriquecimiento.
- Agregada trazabilidad fuente/CA-detalle en `TAREA-001` a `TAREA-005` para que cada hito parta del requerimiento aprobado y no solo del titulo del CA.

## Pendientes no ejecutados
- No se hizo push a `desarrollo` porque la rama local esta divergente frente a `origin/desarrollo` (`ahead 36, behind 25`).
- `desestimado/` queda fuera de git por seguridad. Si se necesita rescatar algo, debe copiarse manualmente a una ruta limpia y revisar credenciales antes de versionar.

## Validaciones
- `docker exec studiamatch-dev python3 .context/crear_tarea.py --listar`
- `docker exec studiamatch-dev python3 -m py_compile .context/crear_tarea.py`
