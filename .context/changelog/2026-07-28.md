# Changelog 2026-07-28

## F9.7 PR-R

- Candidate local v3 `F9.7-PUBLIC-ACCESS-TRIGGER-RETIREMENT-V3-20260728` queda PR-ready sin cerrar F9.7.
- V2 queda historico no promocionable; v3 es el unico successor local promocionable.
- T02 permanece `NOT_EXECUTED`, backup `PLANNED`, writers `INVENTORIED`, Free/Pro `UNCHANGED_BLOCKED`.
- No hubo lectura/escritura Free o Pro, Supabase MCP, Cloudflare, DDL/DML remoto, backup/restore, hold de writers, Edge deploy, pg_net ni package apply.
- Frontend centraliza captura de leads en `leadCaptureCore.ts` y `leadCapture.ts`; el flag `NEXT_PUBLIC_LEAD_CAPTURE_ENABLED` es UX/build-time only y requiere rebuild/redeploy por `output: export`.
- Export frontend agrega marcadores SSR `enabled|disabled|unset`, falla cerrado sin controles PII en `disabled/unset`, valida Playwright hermetico con CORS/OPTIONS/errores accesibles y evita `next/font/google` para builds locales sin red.
- `NEXT_PUBLIC_SUPABASE_URL` se valida como origen Supabase HTTPS limpio, con loopback permitido solo para `sb_publishable_ci_test`; CI/hooks bloquean `sb_publishable_` reales ademas de secret keys.
- Siguiente accion documental: `merge_and_postmerge_replay_then_define_PR_O`.
