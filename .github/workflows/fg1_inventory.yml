name: FG1 - Institution Inventory Management

on:
  # schedule:
  #   - cron: '0 0 1 * *' # ⛔ DESACTIVADO — Hito 1: cron comentado, solo workflow_dispatch
  workflow_dispatch:      # Manual con autorizacion explicita
    inputs:
      resume_authorized:
        description: 'Escribe exactamente true para autorizar FG1 manual en ramas permanentes'
        required: true
        type: boolean
        default: false

concurrency:
  group: studiamatch-fg1-${{ github.ref }}
  cancel-in-progress: false

jobs:
  inventory:
    name: "0. Institution Inventory"
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.resume_authorized == true && contains(fromJSON('["main", "certificacion", "desarrollo"]'), github.ref_name) }}
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    environment:
      name: ${{ (github.ref_name == 'main' && 'Production') || (github.ref_name == 'certificacion' && 'Certification') || 'Development' }}
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      - name: Install Dependencies
        run: pip install -r requirements.txt
      - name: Run Discovery Institutions
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          NEXT_SUPABASE_SECRET_KEY: ${{ secrets.NEXT_SUPABASE_SECRET_KEY }}
          NEXT_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_SUPABASE_PUBLISHABLE_KEY }}
          NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY }}
        run: python scripts/core/discovery_institutions.py
