{
  "name": "studiamatch-g5-trust-broker",
  "version": "0.0.0-f10.9-pr-f",
  "private": true,
  "description": "Repository-only Cloudflare Worker package for the G5 trust broker.",
  "type": "module",
  "scripts": {
    "e1:dry-run": "wrangler deploy --strict --config wrangler.repository-only.jsonc --dry-run --outdir /tmp/studiamatch-g5-e1-dry-run",
    "e1:deploy": "wrangler deploy --strict --config wrangler.repository-only.jsonc"
  },
  "devDependencies": {
    "wrangler": "4.30.0"
  }
}
