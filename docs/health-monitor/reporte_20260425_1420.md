# Reporte de Diagnóstico de Salud - Error 404 y Registros NULL
**Fecha**: 2026-04-25 14:20
**Responsable**: @health-monitor / Antigravity

## 1. Hallazgos Críticos
Se ha identificado una degradación en la integridad de los datos en las tablas `courses` y `cleansed_programs` afectando la experiencia de usuario (Errores 404).

| Institución | Registros Corruptos (Name NULL) | Estado en Frontend |
|-------------|--------------------------------|-------------------|
| Universidad del Pacífico | Todos (Pre-eliminados) | 404 Global |
| Universidad de Lima | 5 detectados | 404 / Slugs con Hash |

## 2. Análisis de Causa Raíz (RCA)
- **Fallo de Cosecha**: El `UniversalHarvester` falló al extraer el título y HTML de las páginas de la UP, posiblemente por bloqueos de red o renderizado dinámico no manejado.
- **Fuga en el Pipeline**: El `CleansingWorker` promocionó registros sin validar la presencia de un nombre real, asignando slugs genéricos.
- **Estructura Fragmentada**: La UP utiliza una página raíz que devuelve 404 pero subpáginas (como `/presentacion/`) que tienen el contenido real. El worker no estaba diseñado para "ascender" esta información.

## 3. Acciones de Remediación (Propuestas)
1. **Purgado**: Eliminar registros con `name IS NULL` de la tabla `courses`. (Pendiente aprobación).
2. **Re-Harvesting**: Forzar la captura de HTML para URLs de UP y U. Lima usando Playwright en modo sigilo.
3. **Consolidación Inteligente**: Aplicar la mejora en `CleansingWorker.py` que ya fue implementada para buscar el mejor nombre entre páginas hermanas.

## 4. Estado de Salud del Proyecto
**CRÍTICO**: La visualización de programas de la UP está actualmente fuera de servicio en el catálogo hasta que se complete el ciclo de re-cosecha y promoción.

---
*Este reporte ha sido generado automáticamente para el seguimiento del SDLC.*
